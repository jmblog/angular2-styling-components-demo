{
  "name": "angular2-styling-components-demo",
  "version": "1.0.0",
  "description": "Demo of Styling Angular2 Components",
  "scripts": {
    "start": "cd app && live-server",
    "deploy": "gh-pages -d app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yoshihide Jimbo",
  "dependencies": {
    "rxjs": "^5.0.0-alpha.14"
  },
  "devDependencies": {
    "gh-pages": "^0.6.0",
    "live-server": "^0.8.2"
  }
}
